apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: persistentvolumeclaim
    namespace: k8s-template
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 2Gi
